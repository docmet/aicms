---
phase: 14-polish-stability
plan: 02
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - frontend/src/app/dashboard/sites/[site_id]/page.tsx
  - frontend/src/components/admin/section-editors/HeroEditor.tsx
  - frontend/src/components/admin/section-editors/FeaturesEditor.tsx
  - frontend/src/components/admin/section-editors/ContactEditor.tsx
  - frontend/src/components/admin/section-editors/PricingEditor.tsx
  - frontend/src/components/admin/section-editors/AboutEditor.tsx
autonomous: true
requirements:
  - PLSH-03

must_haves:
  truths:
    - "Site editor header buttons do not overflow on a 375px viewport — they wrap or stack cleanly"
    - "All section editors are usable on a 375px phone — no horizontal overflow, inputs are reachable"
    - "TabsList max-w-lg does not clip tabs on small phones"
  artifacts:
    - path: "frontend/src/app/dashboard/sites/[site_id]/page.tsx"
      provides: "Responsive site editor page"
      contains: "flex-wrap"
    - path: "frontend/src/components/admin/section-editors/HeroEditor.tsx"
      provides: "Mobile-safe hero editor"
      contains: "sm:grid-cols-2"
  key_links:
    - from: "Site editor header"
      to: "Button row"
      via: "Tailwind responsive classes"
      pattern: "flex-wrap"
    - from: "Section editors"
      to: "grid-cols-2 inputs"
      via: "sm: breakpoint override"
      pattern: "grid-cols-1 sm:grid-cols-2"
---

<objective>
Make the admin site editor fully usable on a 375px mobile viewport. Fix header button overflow, remove max-w-lg from TabsList on mobile, and convert all section editor two-column grids to single-column on mobile.

Purpose: Cold users may access their site from a phone. Section editors are currently unusable at narrow widths due to fixed two-column grids. The header action buttons overflow off-screen on phone.
Output: Responsive site editor page and updated section editor components.
</objective>

<execution_context>
@/Users/docmet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/docmet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-polish-stability/14-01-SUMMARY.md

<interfaces>
<!-- Editors with grid-cols-2/3 that will overflow at 375px — change to grid-cols-1 sm:grid-cols-2 -->

HeroEditor: `<div className="grid grid-cols-2 gap-4">` — wraps Primary Button and Secondary Button inputs
FeaturesEditor: uses `flex gap-2` for items (OK) but may have a grid for section fields — verify
ContactEditor: `grid grid-cols-2 gap-4` for email/phone
PricingEditor: plan cards use `grid-cols-3 gap-2` for name/price/period — most critical
AboutEditor: stats use `grid grid-cols-2 gap-2` (stat number + label pairs) — these are narrow but the pair is per stat, may be OK at 375px; verify

<!-- Site editor page header (approximate current structure) -->
<div className="flex items-center gap-2">
  <Button>Analytics</Button>
  <Button>Share Draft</Button>
  <Button>Preview Draft</Button>
  <Button>Publish</Button>
  <!-- possibly more -->
</div>
<!-- Change outer div to: flex flex-wrap items-center gap-2 -->

<!-- TabsList max-w-lg fix -->
<TabsList className="grid w-full max-w-lg grid-cols-4">
<!-- Change to: className="grid w-full sm:max-w-lg grid-cols-4" -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix site editor page header and TabsList responsiveness</name>
  <files>frontend/src/app/dashboard/sites/[site_id]/page.tsx</files>
  <action>
This file is large (~800+ lines). Make targeted changes only — do not restructure.

**Header button row:** Find the div containing the Analytics, Share Draft, Preview Draft, and Publish buttons. The div currently uses `flex items-center gap-2` or similar. Change it to `flex flex-wrap items-center gap-2` so buttons wrap to a second row on narrow viewports rather than overflowing.

On mobile the buttons should remain functional — do not hide them behind a hamburger. Wrapping is sufficient.

**TabsList:** Find `<TabsList className="grid w-full max-w-lg grid-cols-4">` (the tabs for Pages / Sections / Theme / Settings or similar). Change `max-w-lg` to `sm:max-w-lg` so the full width is used on phones.

**Section card padding:** If section list cards have `p-4` or `p-6` padding on the outer wrapper, ensure they do not have `overflow-hidden` that would clip editor content. No change needed if not present — skip.

After changes: run `./cli.sh typecheck:frontend` and confirm no new errors.
  </action>
  <verify>
    <automated>./cli.sh typecheck:frontend 2>&1 | grep "error TS" | head -10</automated>
  </verify>
  <done>Site editor header wraps on mobile. TabsList is full-width on phones. No TypeScript errors.</done>
</task>

<task type="auto">
  <name>Task 2: Fix grid-cols-2/3 overflow in section editors</name>
  <files>
    frontend/src/components/admin/section-editors/HeroEditor.tsx
    frontend/src/components/admin/section-editors/FeaturesEditor.tsx
    frontend/src/components/admin/section-editors/ContactEditor.tsx
    frontend/src/components/admin/section-editors/PricingEditor.tsx
    frontend/src/components/admin/section-editors/AboutEditor.tsx
  </files>
  <action>
Apply this rule to every `grid-cols-2` and `grid-cols-3` in section editors: prepend `grid-cols-1 sm:` so the grid becomes single-column on mobile.

**Changes by file:**

HeroEditor.tsx:
- Find: `className="grid grid-cols-2 gap-4"` (wraps Primary Button + Secondary Button sections)
- Change to: `className="grid grid-cols-1 sm:grid-cols-2 gap-4"`

FeaturesEditor.tsx:
- Read the file and locate any `grid-cols-2` or `grid-cols-3`. Apply same fix.
- The items list uses `flex gap-2` which is fine on mobile — leave it.

ContactEditor.tsx:
- Find: `grid grid-cols-2 gap-4` (email + phone)
- Change to: `grid grid-cols-1 sm:grid-cols-2 gap-4`

PricingEditor.tsx:
- Find: `grid-cols-3 gap-2` (plan name, price, period — very cramped at 375px)
- Change to: `grid-cols-1 sm:grid-cols-3 gap-2`
- Also find any plan features list grids and apply same fix if present.

AboutEditor.tsx:
- Find: `grid grid-cols-2 gap-2` (stat items)
- Change to: `grid grid-cols-1 sm:grid-cols-2 gap-2`

After changes: run `./cli.sh typecheck:frontend` and `./cli.sh lint:frontend`.
  </action>
  <verify>
    <automated>./cli.sh lint:frontend 2>&1 | grep -E "error|Error" | head -10</automated>
  </verify>
  <done>All section editor grids are single-column on mobile (grid-cols-1) and two/three-column on sm+ breakpoint. No lint or type errors.</done>
</task>

</tasks>

<verification>
Run `./cli.sh typecheck:frontend` — zero errors.
Run `./cli.sh lint:frontend` — zero errors.
Manual: Chrome DevTools → mobile emulation → 375×812. Open a site editor. Verify:
- Header buttons wrap to two lines (do not overflow to the right)
- TabsList is full width
- Hero editor CTA inputs stack vertically
- PricingEditor plan fields stack vertically
- ContactEditor email/phone fields stack vertically
- All inputs are reachable and functional
</verification>

<success_criteria>
- Site editor is navigable at 375px without horizontal scroll
- All section editor grids are responsive (grid-cols-1 sm:grid-cols-N)
- `./cli.sh typecheck:frontend` and `./cli.sh lint:frontend` both pass
</success_criteria>

<output>
After completion, create `.planning/phases/14-polish-stability/14-02-SUMMARY.md`
</output>
